/*Que es una variable
es la representacion de algun lugar en memoria que nosostros vamos na reservar para guardr algun valor

como crear una variable:

primero usariamos la palabra reservada "var" seguido de un "=" y por ultimo la variable que queremos almacenar (por ejemplo un numero, string, etc) y poir ultimo pondriamos un punto y coma (;) que indica que ya terminamos la sentencia (esto se hace por buena practica), ejemplo:

var nombre = "Oscar";

Estados a la hora de generar una variable

    1-Declarar una variable: generar la palabra reservada "var" seguido del nombre de la variable y finalizo con punto y coma
    2-Inicializar la variable: llamamos la variable con su nombre, ponemos "=" y le asignamos su valor

    Nota: podemos declarar e inicializar la variale en la misma linea de cogido


Variables de Array:

    var elementos = ["computadora", "celular"];

    Nota: si nosotros llmamos la variable asi "elementos[1]" nos mostrara el elemento "celular" ya que los arreglos siempre inician en 0 (cero)

Variables tipo objeto:

    var persona = {
        nombre: "Diego",
        edad: 30
    }

    Luego de tener el objeto, lo llamamos (en este caso escribimos persona;) y esto nos mostrara los datos que contiene (al señalarlo)

*/